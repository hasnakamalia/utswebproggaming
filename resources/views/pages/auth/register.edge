@layout.app({title: 'Register'})
<div class="w-full max-w-lg mx-auto p-6">
  <div class="mt-7 bg-pink-50 border border-pink-100 rounded-2xl shadow-md">
    <div class="p-4 sm:p-7">
      <div class="text-center">
        <h1 class="block text-2xl font-extrabold text-pink-500">📝✨ Daftar Akun Baru di Langkah Cerdas Nana 🎓🌈</h1>
        <p class="mt-2 text-sm text-gray-600">
          Udah punya akun? 🤔
          <a class="text-sky-500 font-semibold hover:underline" href="/login">Masuk aja yuk! 🔑</a>
        </p>
      </div>

      <div class="mt-5">
        <form action="/register" method="post">
          {{ csrfField() }}

          <div class="grid gap-y-4">
            <div>
              <label for="name" class="block text-sm font-medium text-pink-600 mb-2">👤 Nama Lengkap</label>
              <input type="text" id="name"
                class="py-3 px-4 block w-full border border-pink-200 rounded-xl text-sm focus:ring-pink-200 focus:outline-none"
                name="name" required value="{{ old('name') || '' }}">

              @inputError('name')
              <p class="text-xs text-red-500 mt-2">⚠️ {{ $messages }} 🥺</p>
              @end
            </div>

            <div>
              <label for="username" class="block text-sm font-medium text-pink-600 mb-2">🎀 Username</label>
              <input type="text" id="username"
                class="py-3 px-4 block w-full border border-pink-200 rounded-xl text-sm focus:ring-pink-200 focus:outline-none"
                name="username" required value="{{ old('username') || '' }}">

              @inputError('username')
              <p class="text-xs text-red-500 mt-2">⚠️ {{ $messages }} 🙈</p>
              @end
            </div>

            <div>
              <label for="email" class="block text-sm font-medium text-pink-600 mb-2">📧 Email</label>
              <input type="email" id="email"
                class="py-3 px-4 block w-full border border-pink-200 rounded-xl text-sm focus:ring-pink-200 focus:outline-none"
                name="email" required value="{{ old('email') || '' }}">

              @inputError('email')
              <p class="text-xs text-red-500 mt-2">⚠️ {{ $messages }} 💌</p>
              @end
            </div>

            <div>
              <label for="password" class="block text-sm font-medium text-sky-600 mb-2">🔑 Password Rahasia</label>
              <input type="password" id="password"
                class="py-3 px-4 block w-full border border-sky-200 rounded-xl text-sm focus:ring-sky-200 focus:outline-none"
                name="password" required>

              @inputError('password')
              <p class="text-xs text-red-500 mt-2">⚠️ {{ $messages }} 🫣</p>
              @end
            </div>

            <div>
              <label for="password_confirmation" class="block text-sm font-medium text-sky-600 mb-2">🔒 Konfirmasi Password</label>
              <input type="password" id="password_confirmation"
                class="py-3 px-4 block w-full border border-sky-200 rounded-xl text-sm focus:ring-sky-200 focus:outline-none"
                name="password_confirmation" required>
            </div>

            <div>
              <button
                class="w-full py-3 px-4 bg-gradient-to-r from-pink-400 to-fuchsia-500 text-white font-bold rounded-full shadow-md hover:brightness-105 transition-all">
                🌟 Daftar Sekarang 🧸✨
              </button>
            </div>
          </div>
        </form>
      </div>

    </div>
  </div>
</div>
@end
