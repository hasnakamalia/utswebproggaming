@layout.app({ title: 'Log in' })
  <div class="w-full max-w-lg mx-auto p-6">
    <div class="mt-7 bg-pink-50 border border-pink-100 rounded-2xl shadow-md">
      <div class="p-6 sm:p-8">
        <div class="text-center">
          <h1 class="text-2xl font-extrabold text-pink-500">🔐✨ Masuk ke Langkah Cerdas Nana 🎓🌈</h1>
          <p class="mt-2 text-sm text-gray-600">
            Belum punya akun? 🧐
            <a class="text-sky-500 font-semibold hover:underline" href="/register">Daftar yuk 🧸🎉</a>
          </p>
        </div>

        <div class="mt-6">
          @flashMessage('notification')
            <div class="mt-2 bg-red-50 border border-red-100 text-sm text-red-600 rounded-md p-3" role="alert">
              ❗ {{ $message.message }} 😔
            </div>
          @end

          <form action="/login" method="post">
            {{ csrfField() }}

            <div class="grid gap-y-5">
              <div>
                <label for="email" class="block text-sm font-medium text-pink-600 mb-2">📧 Email kamu</label>
                <input type="email" id="email"
                  class="py-3 px-4 w-full bg-white border border-pink-200 text-sm rounded-xl focus:ring-2 focus:ring-pink-200 focus:outline-none"
                  name="email" required value="{{ old('email') || '' }}">

                @inputError('email')
                  <p class="text-xs text-red-500 mt-1">⚠️ {{ $messages }} 💌</p>
                @end
              </div>

              <div>
                <label for="password" class="block text-sm font-medium text-sky-600 mb-2">🔑 Password rahasia</label>
                <input type="password" id="password"
                  class="py-3 px-4 w-full bg-white border border-sky-200 text-sm rounded-xl focus:ring-2 focus:ring-sky-200 focus:outline-none"
                  name="password" required>

                @inputError('password')
                  <p class="text-xs text-red-500 mt-1">🔒 {{ $messages }} 😅</p>
                @end
              </div>

              <div>
                <button
                  class="w-full py-3 px-4 bg-gradient-to-r from-sky-300 to-pink-300 text-white font-bold rounded-full shadow-sm hover:brightness-105 transition-all">
                  🚀✨ Masuk Sekarang 💖
                </button>
              </div>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
@end
